<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="GameBib.EditGenrePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GameBib"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="#3C3C3C" Margin="5" Padding="10" CornerRadius="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Column="0" Spacing="10">
            <TextBlock Text="Genre Name" FontSize="16" Foreground="#39FF14"/>
            <TextBox x:Name="GenreNameTextBox" FontSize="16" Foreground="#39FF14" Background="#333333"/>
            <Button x:Name="SaveButton" Content="Save" Click="SaveButton_Click" Background="#39FF14" Foreground="#FFFFFF" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Page>
